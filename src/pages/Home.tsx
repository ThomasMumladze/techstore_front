import React from "react";
import Header from "../layouts/Header";

const Home = () => {
    return (
        <React.Fragment>
            <Header />

            <main style={{ height: "100vh" }}></main>
        </React.Fragment>
    );
};

export default Home;
